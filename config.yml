Pdm2Pcm:
user_pdm_file_path:'path_to_pdm_file.txt'
pcm_sample_rate:8000  
decimation_factor:64  
order:4 

DCRemoval:
cutoff_frequency:100
numtaps=4400

VAD:
frame_duration = 0.01 #10nm frame duration
threshold = 0.1
smoothness = 0
remove_dc = False 
plot_graphs = False

AGC:
frame_duration = 0.01
gain = 0.1

STFT:

NoiseReduction:

PitchEstimation:

SpeechSpeed:

DI:
#decimation and interpolation
  decimation_factor: 4
  interpolation_factor: 5
  decimation_or_interpolation:'interpolation' #'decimation', 'interpolation'
  interpolation_filter_type: 'ZOH' #'ZOH', 'FOH', 'shanon' 

SSB:
  ssb_mode: 'file' #'live', 'file'  

example:
  number_of_frames: 10
  filter: 'lowpass' # 'lowpass', 'highpass', 'bandpass', 'bandstop'
  remove_dc: true
